//@version=6
indicator("VIX Term Structure Pro [v7.6]", overlay=false, max_lines_count=500)

// ╔════════════════════════════════════════════════════════════════════════════╗
// ║                    VIX Term Structure Pro v7.6 使用指南                      ║
// ╠════════════════════════════════════════════════════════════════════════════╣
// ║ 🎯 信号含义 (Signal Meanings)                                               ║
// ║----------------------------------------------------------------------------║
// ║ 🚨 CRASH BUY    │ Score ≥ 6  │ 极端恐慌，罕见机会，激进买入              ║
// ║ 🟢 STRONG BUY   │ Score ≥ 5  │ 多因子共振，强买入信号，建仓              ║
// ║ 🟡 BUY DIP      │ Score ≥ 4  │ 中等恐慌，逢低吸纳，加仓现有头寸          ║
// ║ 🟠 SELL/HEDGE   │ Score ≤ -2 │ 自满/贪婪，考虑减仓或对冲                 ║
// ║ 🔴 STRONG SELL  │ Score ≤ -5 │ 强烈看跌信号，减仓                        ║
// ║ 🔥 EUPHORIA     │ Score ≤ -6 │ 极度贪婪，市场过热，强卖出信号            ║
// ║----------------------------------------------------------------------------║
// ║ 💡 状态提示 (Status)                                                        ║
// ║----------------------------------------------------------------------------║
// ║ WAIT (等待)    │ 🟡 买入侧：评分达标但被过滤，不要急于进场              ║
// ║ HOLD (持有)    │ 🟠 卖出侧：评分达标但被过滤，继续持有仓位              ║
// ║----------------------------------------------------------------------------║
// ║ 📊 仪表盘指标解读                                                           ║
// ║----------------------------------------------------------------------------║
// ║ 指标           │ 看涨信号 🟢              │ 看跌信号 🔴                     ║
// ║----------------------------------------------------------------------------║
// ║ AI Score       │ ≥ 5 (极度恐慌)           │ ≤ -5 (极度贪婪)                 ║
// ║ Term Struct Z  │ < -2.0 (恐慌)           │ > 2.0 (贪婪)                    ║
// ║ VIX Regime     │ LOW VOL (<15)           │ HIGH VOL (>25)                 ║
// ║ Market Trend   │ 🟢SPX 🟢NDX 🟢RUT       │ 🔴SPX 🔴NDX 🔴RUT               ║
// ║ Z Momentum     │ ↓ 恐慌增加              │ ↑ 贪婪增加                      ║
// ╚════════════════════════════════════════════════════════════════════════════╝

// ========== 辅助函数定义 ==========
// 格式化带符号的数字 (Pine Script 不支持 "+#.#;-#.#" 格式)
format_signed_1(val) =>
    (val >= 0 ? "+" : "") + str.tostring(val, "#.#")

format_signed_int(val) =>
    (val >= 0 ? "+" : "") + str.tostring(val, "#")

// ========== 评分函数定义 (模块化) ==========
calc_z_score_points(z_val, mid, strong) =>
    z_val < -strong ? 4 : z_val < -mid ? 2 : z_val > strong ? -4 : z_val > mid ? -2 : 0

calc_contango_points(contango) =>
    contango < 0 ? 2 : contango > 10 ? -1 : 0

calc_basis_points(is_panic, is_calm) =>
    is_panic ? 2 : is_calm ? -1 : 0

calc_skew_points(is_high, is_low) =>
    is_high ? -3 : is_low ? 1 : 0

calc_pcr_points(pcr_val) =>
    na(pcr_val) ? 0 : pcr_val > 1.20 ? 2 : pcr_val < 0.70 ? -2 : 0

calc_vvix_points(vvix_val, use_vvix) =>
    not use_vvix or na(vvix_val) ? 0 : vvix_val > 130 ? 1 : vvix_val < 80 ? -1 : 0

// ========== KAMA 自定义实现 (Kaufman Adaptive Moving Average) ==========
// 由于 ta.kama() 在某些环境不可用，手动实现
calc_kama(src, length, fastLen, slowLen) =>
    // 计算效率比率 (Efficiency Ratio)
    change_sum = math.abs(src - src[length])
    volatility = math.sum(math.abs(src - src[1]), length)
    er = volatility != 0 ? change_sum / volatility : 0.0
    // 计算平滑常数 (Smoothing Constant)
    fast_sc = 2.0 / (fastLen + 1)
    slow_sc = 2.0 / (slowLen + 1)
    sc = math.pow(er * (fast_sc - slow_sc) + slow_sc, 2)
    // 计算 KAMA (递归)
    var float kama = na
    kama := na(kama[1]) ? src : kama[1] + sc * (src - kama[1])
    kama

// ========== 1. 参数设置 ==========

// ────────────────────────────────────────────────────────────────────────────
// 📡 数据源设置 (Data Sources)
// ────────────────────────────────────────────────────────────────────────────
grp_data = "📡 Data Sources"
backtest_mode = input.bool(false, "🔬 Backtest Mode", group=grp_data, tooltip="OFF: 实时模式 Live mode\nON: 回测模式 Backtest mode (无重绘 no repaint)")
sym_vix_input = input.symbol("CBOE:VIX", "VIX Symbol", group=grp_data, tooltip="VIX数据源 VIX data source\n默认 Default: CBOE:VIX")
vix_timeframe = input.string("Daily", "VIX Timeframe", options=["Daily", "Chart"], group=grp_data, tooltip="Daily: 日线收盘价 daily close (稳定 stable)\nChart: 跟随图表 follow chart (实时 real-time)")
sym_pcr_input = input.symbol("INDEX:CPCI", "Put/Call Ratio Symbol", group=grp_data, tooltip="看跌/看涨比率 Put/Call Ratio\n默认 Default: INDEX:CPCI (指数期权 Index options)")
manual_trend_src = input.symbol("SP:SPX", "Manual Trend Source", group=grp_data, tooltip="手动趋势源 Manual trend source\n仅当自动检测关闭时使用 Only used when Auto-Detect is OFF")

// ────────────────────────────────────────────────────────────────────────────
// ⚠️ 策略模式 (Strategy Mode) - 核心设置
// ────────────────────────────────────────────────────────────────────────────
grp_strategy = "⚠️ Strategy Mode (核心设置)"
sens_mode = input.string("Low (Trend/Safe)", "Signal Sensitivity", options=["High (Scalping)", "Normal (Swing)", "Low (Trend/Safe)"], group=grp_strategy, tooltip="High: 高敏感度 Sensitive (短线 scalping)\nNormal: 平衡 Balanced (波段 swing)\nLow: 保守 Conservative (趋势 trend)")
use_trend_filter = input.bool(true, "🛡️ Market Trend Filter", group=grp_strategy, tooltip="ON: 熊市过滤 BUY DIP Filter in bear market\n仅 STRONG BUY 不受限制 Only STRONG BUY allowed")
auto_detect_index = input.bool(true, "Auto-Detect Index (Based on Chart)", group=grp_strategy, tooltip="ON: 自动检测 Auto-detect (QQQ→NDX, 其他→SPX)\nOFF: 使用手动趋势源 Use manual trend source")

// 自适应趋势过滤器设置 (v7.1 新增)
trend_ma_mode = input.string("Adaptive", "Trend MA Mode", options=["Fixed", "Adaptive", "KAMA"], group=grp_strategy, tooltip="Fixed: 固定均线 Fixed MA\nAdaptive: 波动自适应 Vol-adaptive\nKAMA: 效率自适应 Efficiency-adaptive")
trend_ma_type = input.string("SMA", "└─ Fixed MA Type", options=["SMA", "EMA"], group=grp_strategy, tooltip="仅Fixed模式 For Fixed mode only\nSMA: 稳定 Stable / EMA: 灵敏 Responsive")
trend_ma_length_high = input.int(100, "└─ Adaptive Length (High Vol)", minval=20, maxval=200, group=grp_strategy, tooltip="高波动期均线长度 MA length for high vol\n更短=更敏感 Shorter=more sensitive")
trend_ma_length_low = input.int(200, "└─ Adaptive/Fixed Length (Low Vol)", minval=100, maxval=500, group=grp_strategy, tooltip="低波动期/Fixed默认长度 MA length for low vol/Fixed\n更长=更平滑 Longer=smoother")
kama_length = input.int(21, "└─ KAMA Length", minval=5, maxval=50, group=grp_strategy, tooltip="KAMA计算周期 KAMA lookback\n较短=反应更快 Shorter=faster response")

// ────────────────────────────────────────────────────────────────────────────
// 🎯 信号确认 (Signal Confirmation)
// ────────────────────────────────────────────────────────────────────────────
grp_confirm = "🎯 Signal Confirmation"
use_confirmed_signals = input.bool(false, "🔒 Confirmed Signals Only", group=grp_confirm, tooltip="ON: K线收盘确认 Confirm on bar close (稳定 stable)\nOFF: 实时信号 Real-time (盘中报警 intraday alerts)")
use_momentum_confirm = input.bool(true, "Use Momentum Confirmation", group=grp_confirm, tooltip="启用动量确认 Enable momentum confirmation\n需要Z-Score有方向性变化 Require Z-Score direction")
momentum_bars = input.int(3, "Momentum Lookback Bars", minval=1, maxval=10, group=grp_confirm, tooltip="动量计算周期 Momentum lookback period")
z_momentum_neutral_zone = input.float(0.05, "Z Momentum Neutral Zone", minval=0.01, maxval=0.2, step=0.01, group=grp_confirm, tooltip="Z动量中性区 Z momentum neutral zone\n过滤噪音 Filter noise")
use_mtf_confirm = input.bool(false, "Use Weekly MTF Confirmation", group=grp_confirm, tooltip="周线确认 Weekly confirmation\n需要周线Z方向一致 Require weekly Z alignment")
signal_display_cooldown = input.int(5, "Signal Display Cooldown (Bars)", minval=1, maxval=20, group=grp_confirm, tooltip="信号冷却间隔 Signal cooldown (bars)\n避免同类信号频繁触发 Prevent frequent triggers")

// ────────────────────────────────────────────────────────────────────────────
// 🎨 图表样式 (Chart Style)
// ────────────────────────────────────────────────────────────────────────────
grp_chart = "🎨 Chart Style"
show_z      = input.bool(true, "Show Term Struct Z-Score", group=grp_chart, tooltip="显示期限结构Z值 Show term structure Z-Score")
show_skew   = input.bool(false, "Show Scaled SKEW Line", group=grp_chart, tooltip="显示SKEW线 Show SKEW line (缩放显示 scaled)")
show_volume = input.bool(true, "Show Smart Volume", group=grp_chart, tooltip="显示智能成交量 Show smart volume bars")
mid_line_color = input.color(color.yellow, "±1 Line Color", group=grp_chart, tooltip="±1线颜色 ±1 line color")
mid_line_style = input.string("Dashed", "±1 Line Style", options=["Solid", "Dashed", "Dotted"], group=grp_chart, tooltip="±1线样式 ±1 line style")

// ────────────────────────────────────────────────────────────────────────────
// 📋 仪表盘设置 (Dashboard)
// ────────────────────────────────────────────────────────────────────────────
grp_dashboard = "📋 Dashboard"
show_table  = input.bool(true, "Show Dashboard", group=grp_dashboard, tooltip="显示仪表盘 Show dashboard panel")
panel_mode  = input.string("Compact", "Dashboard Mode", options=["Mobile", "Compact", "Full"], group=grp_dashboard, tooltip="Mobile: 极简2行 Minimal 2 rows\nCompact: 精简10行 Essential 10 rows\nFull: 完整15+行 Full 15+ rows")
table_position = input.string("Top Right", "Dashboard Position", options=["Top Right", "Top Left", "Bottom Right", "Bottom Left", "Top Center", "Bottom Center"], group=grp_dashboard, tooltip="仪表盘位置 Dashboard position")
table_size  = input.string("Normal", "Dashboard Text Size", options=["Small", "Normal", "Large"], group=grp_dashboard, tooltip="文字大小 Text size")
bg_transp   = input.int(70, "Dashboard Transparency", minval=0, maxval=100, group=grp_dashboard, tooltip="背景透明度 Background transparency\n0=不透明 Opaque / 100=全透明 Transparent")

// ────────────────────────────────────────────────────────────────────────────
// 📊 统计与警报 (Statistics & Alerts)
// ────────────────────────────────────────────────────────────────────────────
grp_stats = "📊 Statistics & Alerts"
show_signal_stats = input.bool(true, "Show Signal Statistics", group=grp_stats, tooltip="显示信号统计 Show signal statistics")
use_max_profit_stats = input.bool(false, "📈 Show Max Profit Stats", group=grp_stats, tooltip="OFF: 点对点收益 Point-to-point return\nON: 最高收益 Max profit (计算更复杂 more complex)")
stats_lookback_years = input.int(3, "Stats Lookback (Years)", minval=1, maxval=20, group=grp_stats, tooltip="统计回溯年数 Stats lookback years\n仅统计此范围内信号 Only count signals within range")
crash_return_bars = input.int(40, "🚨 CRASH Return Period (Bars)", minval=10, maxval=60, group=grp_stats, tooltip="CRASH信号收益周期 CRASH return period\n约2个月 ~2 months")
strong_return_bars = input.int(20, "🟢 STRONG Return Period (Bars)", minval=10, maxval=40, group=grp_stats, tooltip="STRONG信号收益周期 STRONG return period\n约1个月 ~1 month")
dip_return_bars = input.int(10, "🟡 DIP Return Period (Bars)", minval=5, maxval=20, group=grp_stats, tooltip="DIP信号收益周期 DIP return period\n约2周 ~2 weeks")
enable_smart_alert = input.bool(true, "🔔 Smart Alert", group=grp_stats, tooltip="启用智能警报 Enable Smart Alert\n单条消息汇总所有信号 Aggregated signal message")
alert_cooldown_base = input.int(5, "Alert Cooldown Base (Bars)", minval=1, maxval=20, group=grp_stats, tooltip="基础冷却间隔 Base cooldown\n高波动期减半 Halved in high vol")
alert_freq_mode = input.string("Real-time", "└─ Alert Frequency", options=["Once Per Bar", "Real-time"], group=grp_stats, tooltip="Once Per Bar: 每K线最多一次 Max once per bar\nReal-time: 实时触发 Trigger on every condition")
alert_freq = alert_freq_mode == "Real-time" ? alert.freq_all : alert.freq_once_per_bar

// ────────────────────────────────────────────────────────────────────────────
// ⚙️ 高级设置 (Advanced Settings)
// ────────────────────────────────────────────────────────────────────────────
grp_advanced = "⚙️ Advanced Settings"

// VIX Regime 阈值
vix_low_thresh = input.float(15.0, "VIX Low Vol Threshold", minval=10, maxval=20, group=grp_advanced, tooltip="低波动阈值 Low vol threshold\n低于此值=LOW VOL Below=LOW VOL")
vix_high_thresh = input.float(25.0, "VIX High Vol Threshold", minval=20, maxval=40, group=grp_advanced, tooltip="高波动阈值 High vol threshold\n高于此值=HIGH VOL Above=HIGH VOL")

// 自适应阈值
use_adaptive_thresholds = input.bool(true, "Use Adaptive Thresholds", group=grp_advanced, tooltip="自适应阈值 Adaptive thresholds\n使用百分位动态阈值 Use percentile-based thresholds")
adapt_lookback = input.int(252, "Adaptive Lookback Period", minval=50, maxval=500, group=grp_advanced, tooltip="自适应回溯周期 Adaptive lookback\n252=1年 1 year")

// Z-Score 计算
use_adaptive = input.bool(true, "Use Adaptive Lookback (Z-Score)", group=grp_advanced, tooltip="Z-Score自适应回溯 Adaptive Z lookback\n高波动期用短周期 Shorter in high vol")
len_low      = input.int(252, "Lookback (Low Vol)", minval=50, group=grp_advanced, tooltip="低波动回溯 Low vol lookback\n252=1年 1 year")
len_high     = input.int(126, "Lookback (High Vol)", minval=20, group=grp_advanced, tooltip="高波动回溯 High vol lookback\n126=半年 6 months")
vol_thresh   = input.float(20.0, "Vol Regime Threshold", minval=10, group=grp_advanced, tooltip="波动区间阈值 Vol regime threshold\nVIX高于此值=高波动期 Above=high vol")

// SKEW 设置
skew_mode    = input.string("Relative (Z-Score)", "SKEW Logic Mode", options=["Relative (Z-Score)", "Percentile (Adaptive)", "Absolute (>140)"], group=grp_advanced, tooltip="SKEW判断模式 SKEW logic mode\nZ-Score: 相对标准差 / Percentile: 百分位 / Absolute: 固定值")
skew_len     = input.int(126, "SKEW Lookback Period", minval=20, group=grp_advanced, tooltip="SKEW回溯周期 SKEW lookback\n126=半年 6 months")

// 成交量设置
vol_avg_len  = input.int(20, "Volume Avg Length", minval=5, group=grp_advanced, tooltip="成交量均线长度 Volume MA length")

// ────────────────────────────────────────────────────────────────────────────
// 📈 VVIX 设置 (Optional)
// ────────────────────────────────────────────────────────────────────────────
grp_vvix = "📈 VVIX (Optional)"
use_vvix = input.bool(false, "Enable VVIX Integration", group=grp_vvix, tooltip="启用VVIX Enable VVIX\nVVIX=VIX的波动率 VIX of VIX")
vvix_threshold_mode = input.string("Z-Score", "VVIX Threshold Mode", options=["Fixed", "Percentile", "Z-Score"], group=grp_vvix, tooltip="Fixed: 固定阈值 Fixed (130/80)\nPercentile: 百分位 90/10 percentile\nZ-Score: 标准差 ±2 std dev")
vvix_z_thresh = input.float(2.0, "└─ Z-Score Threshold", minval=1.0, maxval=3.0, step=0.1, group=grp_vvix, tooltip="VVIX Z阈值 Z threshold\n>阈值=恐慌 Panic / <-阈值=平静 Calm")
vvix_panic_thresh = input.float(130.0, "└─ Fixed Panic Threshold", minval=100, maxval=160, group=grp_vvix, tooltip="Fixed模式恐慌阈值 Fixed panic threshold\n仅Fixed模式 For Fixed mode only")
vvix_calm_thresh = input.float(80.0, "└─ Fixed Calm Threshold", minval=60, maxval=100, group=grp_vvix, tooltip="Fixed模式平静阈值 Fixed calm threshold\n仅Fixed模式 For Fixed mode only")

// ────────────────────────────────────────────────────────────────────────────
// 🔬 VIX Characteristics (VIX特性分析) - v7.5 新增
// ────────────────────────────────────────────────────────────────────────────
grp_vix_char = "🔬 VIX Characteristics"

// 总开关
use_vix_characteristics = input.bool(true, "Enable VIX Characteristics", group=grp_vix_char, tooltip="启用VIX特性分析参与评分\nEnable VIX characteristics in scoring")
show_vix_characteristics = input.bool(true, "Show in Dashboard", group=grp_vix_char, tooltip="在仪表盘显示VIX特性指标\nShow VIX characteristics in dashboard")

// 1. 均值回归设置
use_mean_reversion = input.bool(true, "├─ Mean Reversion Analysis", group=grp_vix_char, tooltip="VIX均值回归分析\nVIX长期均值约19-20，极端值必然回归")
mean_reversion_lookback = input.int(252, "│  └─ Lookback Period", minval=100, maxval=500, group=grp_vix_char, tooltip="均值回归计算周期 Lookback\n252=1年 1 year")

// 2. Backwardation持续天数
use_backwardation_duration = input.bool(true, "├─ Backwardation Duration", group=grp_vix_char, tooltip="追踪Backwardation持续天数\n通常5-7天后是较好买点")
backwardation_optimal_days = input.int(5, "│  └─ Optimal Entry Days", minval=3, maxval=10, group=grp_vix_char, tooltip="Backwardation最佳入场天数\n低于此天数信号减弱")

// 3. 非对称动量
use_asymmetric_momentum = input.bool(true, "├─ Asymmetric Momentum", group=grp_vix_char, tooltip="非对称动量确认\nVIX上涨快下跌慢，需确认恐慌消退")
panic_spike_threshold = input.float(15.0, "│  ├─ Panic Spike (1D %)", minval=10, maxval=30, group=grp_vix_char, tooltip="单日恐慌飙升阈值%\nPanic spike threshold")
panic_subside_threshold = input.float(20.0, "│  └─ Subside Confirm (%)", minval=10, maxval=40, group=grp_vix_char, tooltip="恐慌消退确认跌幅%\n需连续3日下跌且累计达此幅度")

// 4. VIX-SPX背离
use_divergence = input.bool(true, "├─ VIX-SPX Divergence", group=grp_vix_char, tooltip="VIX与SPX背离检测\n背离往往预示趋势转折")
divergence_lookback = input.int(5, "│  └─ Divergence Lookback", minval=3, maxval=10, group=grp_vix_char, tooltip="背离检测周期（天）\nDivergence lookback days")

// 5. 波动聚集
use_vol_clustering = input.bool(true, "└─ Volatility Clustering", group=grp_vix_char, tooltip="波动聚集状态检测\n识别危机/平静/收敛等状态")
vol_cluster_short = input.int(10, "   ├─ Short Vol Period", minval=5, maxval=20, group=grp_vix_char, tooltip="短期波动计算周期\nShort-term volatility period")
vol_cluster_long = input.int(60, "   └─ Long Vol Period", minval=30, maxval=120, group=grp_vix_char, tooltip="长期波动计算周期\nLong-term volatility period")

// ────────────────────────────────────────────────────────────────────────────
// 内部计算参数（基于策略模式）
// ────────────────────────────────────────────────────────────────────────────
smooth_dyn = sens_mode == "High (Scalping)" ? 3 : sens_mode == "Normal (Swing)" ? 5 : 8
thresh_strong = sens_mode == "High (Scalping)" ? 2.0 : sens_mode == "Normal (Swing)" ? 2.2 : 2.5
thresh_mid = sens_mode == "High (Scalping)" ? 1.0 : sens_mode == "Normal (Swing)" ? 1.2 : 1.5
min_score_buy = sens_mode == "High (Scalping)" ? 2 : sens_mode == "Normal (Swing)" ? 3 : 4

t_size = table_size == "Small" ? size.small : table_size == "Large" ? size.large : size.normal
is_mobile = panel_mode == "Mobile"
is_compact = panel_mode == "Compact"

// ========== 2. 数据获取 ==========
// Backtest Mode OFF: 使用 lookahead_on 获取当日实时数据
// Backtest Mode ON: 禁用 lookahead，仅使用已确认的历史数据
vix_tf = vix_timeframe == "Daily" ? "D" : ""
lookahead_setting = backtest_mode ? barmerge.lookahead_off : barmerge.lookahead_on

vix     = request.security(sym_vix_input, vix_tf, close, lookahead=lookahead_setting, ignore_invalid_symbol=true)
vx1     = request.security("CBOE:VX1!", "D", close, lookahead=lookahead_setting, ignore_invalid_symbol=true)
vx2     = request.security("CBOE:VX2!", "D", close, lookahead=lookahead_setting, ignore_invalid_symbol=true)
vx1_vol = request.security("CBOE:VX1!", "D", volume, lookahead=lookahead_setting, ignore_invalid_symbol=true)
skew    = request.security("CBOE:SKEW", "D", close, lookahead=lookahead_setting, ignore_invalid_symbol=true)
pcr     = request.security(sym_pcr_input, "D", close, lookahead=lookahead_setting, ignore_invalid_symbol=true)

// VVIX 数据 (可选)
vvix = use_vvix ? request.security("CBOE:VVIX", "D", close, lookahead=lookahead_setting, ignore_invalid_symbol=true) : na

// 自动检测逻辑：如果图表是 QQQ/NQ/NDX 相关，用 NDX；如果是 IWM/RUT/RTY 相关，用 RUT；否则用 SPX
is_nasdaq_chart = str.contains(str.upper(syminfo.ticker), "QQQ") or 
                  str.contains(str.upper(syminfo.ticker), "NDX") or 
                  str.contains(str.upper(syminfo.ticker), "NQ")
is_russell_chart = str.contains(str.upper(syminfo.ticker), "IWM") or 
                   str.contains(str.upper(syminfo.ticker), "RUT") or 
                   str.contains(str.upper(syminfo.ticker), "RTY")

// ========== 2.1 自适应趋势均线计算 (v7.1 新增) ==========
// 根据 VIX 水平确定自适应均线长度
is_high_vol_regime = not na(vix) and vix > vol_thresh
adaptive_ma_len = is_high_vol_regime ? trend_ma_length_high : trend_ma_length_low

// 获取各指数价格和趋势均线
spx_close = request.security("SP:SPX", "D", close, ignore_invalid_symbol=true)
ndx_close = request.security("NASDAQ:NDX", "D", close, ignore_invalid_symbol=true)
rut_close = request.security("TVC:RUT", "D", close, ignore_invalid_symbol=true)
manual_close = request.security(manual_trend_src, "D", close, ignore_invalid_symbol=true)

// 根据模式获取不同的趋势均线
// Fixed 模式：使用用户选择的 SMA/EMA 和固定长度
spx_ma_fixed = request.security("SP:SPX", "D", trend_ma_type == "EMA" ? ta.ema(close, trend_ma_length_low) : ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)
ndx_ma_fixed = request.security("NASDAQ:NDX", "D", trend_ma_type == "EMA" ? ta.ema(close, trend_ma_length_low) : ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)
rut_ma_fixed = request.security("TVC:RUT", "D", trend_ma_type == "EMA" ? ta.ema(close, trend_ma_length_low) : ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)
manual_ma_fixed = request.security(manual_trend_src, "D", trend_ma_type == "EMA" ? ta.ema(close, trend_ma_length_low) : ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)

// Adaptive 模式：根据 VIX 水平动态选择长度（使用 SMA）
spx_ma_adapt_high = request.security("SP:SPX", "D", ta.sma(close, trend_ma_length_high), ignore_invalid_symbol=true)
spx_ma_adapt_low = request.security("SP:SPX", "D", ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)
ndx_ma_adapt_high = request.security("NASDAQ:NDX", "D", ta.sma(close, trend_ma_length_high), ignore_invalid_symbol=true)
ndx_ma_adapt_low = request.security("NASDAQ:NDX", "D", ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)
rut_ma_adapt_high = request.security("TVC:RUT", "D", ta.sma(close, trend_ma_length_high), ignore_invalid_symbol=true)
rut_ma_adapt_low = request.security("TVC:RUT", "D", ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)
manual_ma_adapt_high = request.security(manual_trend_src, "D", ta.sma(close, trend_ma_length_high), ignore_invalid_symbol=true)
manual_ma_adapt_low = request.security(manual_trend_src, "D", ta.sma(close, trend_ma_length_low), ignore_invalid_symbol=true)

// KAMA 模式：Kaufman 自适应均线 (使用自定义实现)
spx_ma_kama = request.security("SP:SPX", "D", calc_kama(close, kama_length, 2, 30), ignore_invalid_symbol=true)
ndx_ma_kama = request.security("NASDAQ:NDX", "D", calc_kama(close, kama_length, 2, 30), ignore_invalid_symbol=true)
rut_ma_kama = request.security("TVC:RUT", "D", calc_kama(close, kama_length, 2, 30), ignore_invalid_symbol=true)
manual_ma_kama = request.security(manual_trend_src, "D", calc_kama(close, kama_length, 2, 30), ignore_invalid_symbol=true)

// 根据模式选择最终使用的均线
spx_trend_ma = trend_ma_mode == "KAMA" ? spx_ma_kama : trend_ma_mode == "Adaptive" ? (is_high_vol_regime ? spx_ma_adapt_high : spx_ma_adapt_low) : spx_ma_fixed
ndx_trend_ma = trend_ma_mode == "KAMA" ? ndx_ma_kama : trend_ma_mode == "Adaptive" ? (is_high_vol_regime ? ndx_ma_adapt_high : ndx_ma_adapt_low) : ndx_ma_fixed
rut_trend_ma = trend_ma_mode == "KAMA" ? rut_ma_kama : trend_ma_mode == "Adaptive" ? (is_high_vol_regime ? rut_ma_adapt_high : rut_ma_adapt_low) : rut_ma_fixed
manual_trend_ma = trend_ma_mode == "KAMA" ? manual_ma_kama : trend_ma_mode == "Adaptive" ? (is_high_vol_regime ? manual_ma_adapt_high : manual_ma_adapt_low) : manual_ma_fixed

valid_data = not na(vix) and not na(vx1) and not na(vx2)

// ========== 3. 核心计算 ==========
// SPX 趋势判断（使用自适应趋势均线）
is_spx_bull = not na(spx_close) ? (spx_close > spx_trend_ma) : true
// NDX 趋势判断
is_ndx_bull = not na(ndx_close) ? (ndx_close > ndx_trend_ma) : true
// RUT 趋势判断
is_rut_bull = not na(rut_close) ? (rut_close > rut_trend_ma) : true

// 主趋势判断（用于评分系统的过滤）
is_primary_bull = auto_detect_index ? 
                  (is_russell_chart ? is_rut_bull : is_nasdaq_chart ? is_ndx_bull : is_spx_bull) : 
                  (not na(manual_close) ? (manual_close > manual_trend_ma) : true)

// 趋势状态文本（三指数合并显示）
spx_emoji = is_spx_bull ? "🟢" : "🔴"
ndx_emoji = is_ndx_bull ? "🟢" : "🔴"
rut_emoji = is_rut_bull ? "🟢" : "🔴"
trend_status_txt = spx_emoji + "SPX " + ndx_emoji + "NDX " + rut_emoji + "RUT"
trend_color = is_spx_bull and is_ndx_bull and is_rut_bull ? color.green : 
              not is_spx_bull and not is_ndx_bull and not is_rut_bull ? color.red : color.orange

// 用于评分的 is_bull_market（使用主趋势）
is_bull_market = is_primary_bull

// VIX Regime 判断
vix_regime = vix < vix_low_thresh ? 1 : vix > vix_high_thresh ? 3 : 2
vix_regime_txt = vix_regime == 1 ? "LOW VOL" : vix_regime == 3 ? "HIGH VOL" : "NORMAL"
vix_regime_color = vix_regime == 1 ? color.lime : vix_regime == 3 ? color.red : color.silver

term_spread = valid_data ? (vx2 - vx1) : 0.0
contango_pct = valid_data and vx1 != 0 ? ((vx2 / vx1) - 1) * 100 : 0.0

// VIX/VX1 Basis（现货溢价）- 恐慌领先指标
vix_basis = valid_data and vx1 != 0 ? ((vix / vx1) - 1) * 100 : 0.0

// 自适应阈值计算
vix_basis_pct_high = ta.percentile_linear_interpolation(vix_basis, adapt_lookback, 90)
vix_basis_pct_low = ta.percentile_linear_interpolation(vix_basis, adapt_lookback, 10)
pcr_pct_high = ta.percentile_linear_interpolation(pcr, adapt_lookback, 90)
pcr_pct_low = ta.percentile_linear_interpolation(pcr, adapt_lookback, 10)

// 根据设置选择阈值模式
is_basis_panic = use_adaptive_thresholds ? (vix_basis > vix_basis_pct_high) : (vix_basis > 5)
is_basis_calm  = use_adaptive_thresholds ? (vix_basis < vix_basis_pct_low) : (vix_basis < -3)

is_high_vol = valid_data and (vix > vol_thresh)
lookback    = use_adaptive ? (is_high_vol ? len_high : len_low) : len_low
avg   = ta.sma(term_spread, lookback)
stdev = ta.stdev(term_spread, lookback)
raw_z = (valid_data and stdev != 0) ? (term_spread - avg) / stdev : 0.0
z     = ta.ema(raw_z, smooth_dyn)

// Z-Score 动量（变化率）- 带中性区域
z_roc = z - z[momentum_bars]
z_momentum_bullish = z_roc < -z_momentum_neutral_zone  // Z 在下降超过阈值（恐慌加剧）
z_momentum_bearish = z_roc > z_momentum_neutral_zone   // Z 在上升超过阈值（贪婪加剧）
z_momentum_flat = math.abs(z_roc) <= z_momentum_neutral_zone  // 中性区域

// 周线 MTF 确认
z_weekly = request.security(syminfo.tickerid, "W", z, ignore_invalid_symbol=true)
is_weekly_buy_aligned = not na(z_weekly) ? z_weekly < -thresh_mid : true
is_weekly_sell_aligned = not na(z_weekly) ? z_weekly > thresh_mid : true

// Smart SKEW - 优化：支持百分位数模式
skew_avg   = ta.sma(skew, skew_len)
skew_stdev = ta.stdev(skew, skew_len)
skew_z     = (skew - skew_avg) / skew_stdev

// SKEW 百分位数计算（更自适应）
skew_pct_high = ta.percentile_linear_interpolation(skew, 252, 90)
skew_pct_low  = ta.percentile_linear_interpolation(skew, 252, 10)

skew_trig_high = sens_mode == "High (Scalping)" ? 1.5 : 1.8 

// 根据模式选择 SKEW 判断逻辑
is_skew_high = skew_mode == "Relative (Z-Score)" ? (skew_z > skew_trig_high) : 
               skew_mode == "Percentile (Adaptive)" ? (skew > skew_pct_high) : 
               (skew > 145)
is_skew_low  = skew_mode == "Relative (Z-Score)" ? (skew_z < -1.5) : 
               skew_mode == "Percentile (Adaptive)" ? (skew < skew_pct_low) : 
               (skew < 110)
scaled_skew = not na(skew) ? (skew - 100) / 10 : na

// VVIX 自适应阈值计算 (v7.1 新增)
vvix_avg = ta.sma(vvix, 252)
vvix_stdev = ta.stdev(vvix, 252)
vvix_z = not na(vvix) and vvix_stdev != 0 ? (vvix - vvix_avg) / vvix_stdev : 0.0
vvix_pct_high = ta.percentile_linear_interpolation(vvix, 252, 90)
vvix_pct_low  = ta.percentile_linear_interpolation(vvix, 252, 10)

// VVIX 判断 - 支持三种模式
vvix_panic_check = vvix_threshold_mode == "Z-Score" ? vvix_z > vvix_z_thresh : vvix_threshold_mode == "Percentile" ? vvix > vvix_pct_high : vvix > vvix_panic_thresh
vvix_calm_check = vvix_threshold_mode == "Z-Score" ? vvix_z < -vvix_z_thresh : vvix_threshold_mode == "Percentile" ? vvix < vvix_pct_low : vvix < vvix_calm_thresh
is_vvix_panic = use_vvix and not na(vvix) and vvix_panic_check
is_vvix_calm = use_vvix and not na(vvix) and vvix_calm_check

// Smart Volume - 使用滚动窗口最大值
vol_ma = ta.sma(vx1_vol, vol_avg_len)
vol_max_rolling = ta.highest(vx1_vol, 252)  // 滚动252日最大值
is_vol_spike  = vx1_vol >= vol_ma * 2.5
is_vol_high   = vx1_vol >= vol_ma * 1.5
col_vol = is_vol_spike ? color.new(color.yellow, 30) : is_vol_high ? color.new(color.aqua, 60) : color.new(color.gray, 90)
vol_visual_boost = is_vol_spike ? 1.2 : 1.0

// ========== 3.5 VIX Characteristics 分析 (v7.5 新增) ==========

// ----- 1. 均值回归距离 (Mean Reversion) -----
vix_long_sma = ta.sma(vix, mean_reversion_lookback)
vix_long_stdev = ta.stdev(vix, mean_reversion_lookback)
vix_mean_distance = vix_long_stdev != 0 ? (vix - vix_long_sma) / vix_long_stdev : 0.0

// 均值回归评分: VIX极高(>2σ)看涨(必然回归), VIX极低(<-1.5σ)看跌
mean_reversion_score = not use_vix_characteristics or not use_mean_reversion ? 0 : vix_mean_distance > 3.0 ? 3 : vix_mean_distance > 2.0 ? 2 : vix_mean_distance > 1.5 ? 1 : vix_mean_distance < -1.5 ? -2 : vix_mean_distance < -1.0 ? -1 : 0

// ----- 2. Backwardation 持续天数追踪 -----
// 前端Backwardation: VIX > VX1
is_front_backwardation = valid_data and vix > vx1
// 完全Backwardation: VIX > VX1 > VX2
is_full_backwardation = valid_data and vix > vx1 and vx1 > vx2

var int backwardation_days = 0
if is_front_backwardation
    backwardation_days += 1
else
    backwardation_days := 0

// Backwardation评分: 持续5天以上开始加分，10天以上强力加分
backwardation_duration_score = not use_vix_characteristics or not use_backwardation_duration ? 0 : backwardation_days >= 10 ? 3 : backwardation_days >= backwardation_optimal_days ? 2 : backwardation_days >= 3 ? 1 : 0

// Backwardation状态文本
backwardation_status = backwardation_days >= 10 ? "EXTREME (" + str.tostring(backwardation_days) + "D)" : backwardation_days >= backwardation_optimal_days ? "OPTIMAL (" + str.tostring(backwardation_days) + "D)" : backwardation_days >= 1 ? "EARLY (" + str.tostring(backwardation_days) + "D)" : "CONTANGO"
backwardation_color = backwardation_days >= 10 ? color.lime : backwardation_days >= backwardation_optimal_days ? color.green : backwardation_days >= 1 ? color.yellow : color.gray

// ----- 3. 非对称动量（恐慌消退确认）-----
// VIX单日变化率
vix_daily_change = not na(vix) and not na(vix[1]) and vix[1] != 0 ? (vix / vix[1] - 1) * 100 : 0.0
// VIX 5日变化率
vix_5d_change = not na(vix) and not na(vix[5]) and vix[5] != 0 ? (vix / vix[5] - 1) * 100 : 0.0

// 恐慌爆发检测: 单日涨幅>阈值 或 5日涨幅>40%
is_panic_spike_detected = vix_daily_change > panic_spike_threshold or vix_5d_change > 40

// 恐慌消退检测: 连续3日下跌 + 从近期高点累计跌幅达标
vix_falling_streak = not na(vix) and vix < vix[1] and vix[1] < vix[2] and vix[2] < vix[3]
vix_recent_high = ta.highest(vix, 10)
vix_drop_from_peak = vix_recent_high != 0 ? (vix_recent_high - vix) / vix_recent_high * 100 : 0.0
is_panic_subsiding = vix_falling_streak and vix_drop_from_peak > panic_subside_threshold

// 非对称动量评分
asymmetric_momentum_score = not use_vix_characteristics or not use_asymmetric_momentum ? 0 : is_panic_subsiding and vix > vix_high_thresh ? 2 : is_panic_subsiding ? 1 : is_panic_spike_detected and not is_panic_subsiding ? -1 : 0

// 恐慌状态文本
panic_status = is_panic_spike_detected and not is_panic_subsiding ? "🔥 SPIKING" : is_panic_subsiding ? "✅ SUBSIDING" : vix_drop_from_peak > 10 ? "↓ DECLINING" : "→ STABLE"
panic_status_color = is_panic_spike_detected and not is_panic_subsiding ? color.red : is_panic_subsiding ? color.lime : color.gray

// ----- 4. VIX-SPX 背离检测 -----
spx_return_div = not na(spx_close) and not na(spx_close[divergence_lookback]) and spx_close[divergence_lookback] != 0 ? (spx_close - spx_close[divergence_lookback]) / spx_close[divergence_lookback] * 100 : 0.0
vix_return_div = not na(vix) and not na(vix[divergence_lookback]) and vix[divergence_lookback] != 0 ? (vix - vix[divergence_lookback]) / vix[divergence_lookback] * 100 : 0.0

// 看涨背离: SPX下跌但VIX也下跌（恐慌消退信号）
is_bullish_divergence = spx_return_div < -2 and vix_return_div < 0
// 看跌背离: SPX上涨但VIX也上涨（隐藏风险）
is_bearish_divergence = spx_return_div > 2 and vix_return_div > 10

// VIX对SPX的反应强度（凸性）: 正常SPX跌1%则VIX涨4-5%，过度则>8
vix_convexity = spx_return_div < -0.5 ? math.abs(vix_return_div / spx_return_div) : 0.0
is_excessive_convexity = vix_convexity > 8

// 背离评分
divergence_score = not use_vix_characteristics or not use_divergence ? 0 : is_bullish_divergence ? 2 : is_excessive_convexity and vix > 25 ? 1 : is_bearish_divergence ? -2 : 0

// 背离状态文本
divergence_status = is_bullish_divergence ? "🟢 BULLISH" : is_bearish_divergence ? "🔴 BEARISH" : is_excessive_convexity ? "⚡ OVERREACT" : "→ NORMAL"
divergence_status_color = is_bullish_divergence ? color.lime : is_bearish_divergence ? color.red : is_excessive_convexity ? color.yellow : color.gray

// ----- 5. 波动聚集状态机 -----
// VIX的波动率（VIX of VIX概念）
vix_vol_short_calc = ta.stdev(vix, vol_cluster_short)
vix_vol_long_calc = ta.stdev(vix, vol_cluster_long)
vol_cluster_ratio = vix_vol_long_calc != 0 ? vix_vol_short_calc / vix_vol_long_calc : 1.0

// 波动状态分类
// CRISIS(5): 波动急剧放大 (>2x)
// ELEVATED(4): 波动扩大 (1.5-2x)
// NORMAL(3): 正常 (0.8-1.5x)
// DECLINING(2): 波动收敛 (0.5-0.8x) - 好的买入窗口
// SUPPRESSED(1): 极度平静 (<0.5x) - 警惕突变
vol_state_id = vol_cluster_ratio > 2.0 ? 5 : vol_cluster_ratio > 1.5 ? 4 : vol_cluster_ratio > 0.8 ? 3 : vol_cluster_ratio > 0.5 ? 2 : 1

vol_state_txt = vol_state_id == 5 ? "CRISIS" : vol_state_id == 4 ? "ELEVATED" : vol_state_id == 3 ? "NORMAL" : vol_state_id == 2 ? "DECLINING" : "SUPPRESSED"

vol_state_color = vol_state_id == 5 ? color.red : vol_state_id == 4 ? color.orange : vol_state_id == 3 ? color.gray : vol_state_id == 2 ? color.lime : color.yellow

// 极度平静天数追踪（用于预警）
var int suppressed_days = 0
if vol_state_id == 1
    suppressed_days += 1
else
    suppressed_days := 0

// 波动聚集评分
vol_clustering_score = not use_vix_characteristics or not use_vol_clustering ? 0 : vol_state_id == 2 and vix > 20 ? 2 : vol_state_id == 2 ? 1 : vol_state_id == 1 and suppressed_days > 30 ? -2 : vol_state_id == 5 ? 1 : 0

// ----- VIX特性综合评分 -----
vix_characteristics_total = mean_reversion_score + backwardation_duration_score + asymmetric_momentum_score + divergence_score + vol_clustering_score

// ========== 4. AI 信号算法 (Enhanced v7.0 - 模块化) ==========
var float score = 0.0

// 使用模块化评分函数 (v7.5: 包含VIX特性评分)
score := calc_z_score_points(z, thresh_mid, thresh_strong) + calc_contango_points(contango_pct) + calc_basis_points(is_basis_panic, is_basis_calm) + calc_skew_points(is_skew_high, is_skew_low) + calc_pcr_points(pcr) + calc_vvix_points(vvix, use_vvix) + (is_vol_spike and z < -thresh_mid ? 1 : 0) + (use_momentum_confirm and z < -thresh_mid and z_momentum_bullish ? 1 : 0) + (use_momentum_confirm and z > thresh_mid and z_momentum_bearish ? -1 : 0) + (use_mtf_confirm and z < -thresh_mid and is_weekly_buy_aligned ? 1 : 0) + (use_mtf_confirm and z > thresh_mid and is_weekly_sell_aligned ? -1 : 0) + (use_trend_filter and not is_bull_market ? -2 : 0) + (use_vix_characteristics ? vix_characteristics_total : 0)

// ========== 5. 三级买入信号 (v7.0 新增, v7.3 优化) ==========
// 基于 Score 评级的信号触发（首次进入该评级时触发）
is_crash_buy_raw = score >= 6 and score[1] < 6
is_strong_buy_raw = score >= 5 and score < 6 and score[1] < 5
is_buy_dip_raw = score >= min_score_buy and score < 5 and score[1] < min_score_buy

// 卖出信号原始判断
is_euphoria_sell_raw = score <= -6 and score[1] > -6
is_strong_sell_raw = score <= -5 and score > -6 and score[1] > -5
is_sell_hedge_raw = score <= -2 and score > -5 and score[1] > -2

// 信号确认机制 (v7.1 新增) - 仅在 K 线确认后触发
is_crash_buy = use_confirmed_signals ? (is_crash_buy_raw and barstate.isconfirmed) : is_crash_buy_raw
is_strong_buy = use_confirmed_signals ? (is_strong_buy_raw and barstate.isconfirmed) : is_strong_buy_raw
is_buy_dip_base = use_confirmed_signals ? (is_buy_dip_raw and barstate.isconfirmed) : is_buy_dip_raw
is_euphoria_sell = use_confirmed_signals ? (is_euphoria_sell_raw and barstate.isconfirmed) : is_euphoria_sell_raw
is_strong_sell = use_confirmed_signals ? (is_strong_sell_raw and barstate.isconfirmed) : is_strong_sell_raw
is_sell_hedge_base = use_confirmed_signals ? (is_sell_hedge_raw and barstate.isconfirmed) : is_sell_hedge_raw

// ===== v7.3 新增：信号冷却追踪变量 =====
var int last_sell_hedge_bar = 0
var int last_buy_dip_bar = 0

// ===== v7.3 新增：增强版 sell_hedge/buy_dip 信号过滤 =====
// sell_hedge 增强条件：
// 1. VIX Regime 不是 LOW VOL（低波动期不需要对冲）
// 2. Z Momentum 上升（贪婪加剧）或 Weekly 卖出对齐
// 3. 满足信号冷却间隔
is_sell_hedge_momentum_ok = z_momentum_bearish or (use_mtf_confirm and is_weekly_sell_aligned) or not use_momentum_confirm
is_sell_hedge_cooldown_ok = bar_index - last_sell_hedge_bar > signal_display_cooldown
is_sell_hedge = is_sell_hedge_base and vix_regime != 1 and is_sell_hedge_momentum_ok and is_sell_hedge_cooldown_ok

// buy_dip 增强条件：
// 1. VIX Regime 不是 HIGH VOL（高波动期等待更强信号）
// 2. Z Momentum 下降（恐慌加剧）或 Weekly 买入对齐
// 3. 满足信号冷却间隔
is_buy_dip_momentum_ok = z_momentum_bullish or (use_mtf_confirm and is_weekly_buy_aligned) or not use_momentum_confirm
is_buy_dip_cooldown_ok = bar_index - last_buy_dip_bar > signal_display_cooldown
is_buy_dip = is_buy_dip_base and vix_regime != 3 and is_buy_dip_momentum_ok and is_buy_dip_cooldown_ok

// 更新冷却追踪
if is_sell_hedge
    last_sell_hedge_bar := bar_index
if is_buy_dip
    last_buy_dip_bar := bar_index

// 趋势过滤
is_signal_allowed = use_trend_filter ? is_bull_market : true

// 最终信号（用于 Alert 和图表标签）
final_crash_buy = is_crash_buy  // CRASH BUY 不受趋势过滤限制
final_strong_buy = is_strong_buy  // STRONG BUY 不受趋势过滤限制
final_buy_dip = is_buy_dip and is_signal_allowed  // BUY DIP 受趋势过滤 + v7.3 增强过滤

final_buy_signal = final_crash_buy or final_strong_buy or final_buy_dip
final_sell_signal = is_euphoria_sell or is_strong_sell or is_sell_hedge

// ========== 6. 信号统计 (v7.0 新增) ==========
// 计算统计回望范围（按年，日线约252根K线/年）
stats_lookback_bars = stats_lookback_years * 252
// 修复：使用 last_bar_index 计算回望范围（相对于图表最后一根K线）
stats_start_bar = last_bar_index > stats_lookback_bars ? (last_bar_index - stats_lookback_bars) : 0
is_within_stats_range = bar_index >= stats_start_bar

// 使用 var 累积统计各级别信号次数
var int crash_buy_count = 0
var int strong_buy_count = 0
var int buy_dip_count = 0
var int sell_count = 0

// 累积收益统计
var float crash_buy_return_sum = 0.0
var float strong_buy_return_sum = 0.0
var float buy_dip_return_sum = 0.0
var int crash_buy_return_count = 0
var int strong_buy_return_count = 0
var int buy_dip_return_count = 0

// 每次重新计算时重置计数（因为 last_bar_index 变化会改变范围）
if barstate.isfirst
    crash_buy_count := 0
    strong_buy_count := 0
    buy_dip_count := 0
    sell_count := 0
    crash_buy_return_sum := 0.0
    strong_buy_return_sum := 0.0
    buy_dip_return_sum := 0.0
    crash_buy_return_count := 0
    strong_buy_return_count := 0
    buy_dip_return_count := 0

// 统计信号次数（仅在回望范围内）
if final_crash_buy and is_within_stats_range
    crash_buy_count += 1
if final_strong_buy and is_within_stats_range
    strong_buy_count += 1
if final_buy_dip and is_within_stats_range
    buy_dip_count += 1
if final_sell_signal and is_within_stats_range
    sell_count += 1

// 计算信号后分级回报（使用 SPX 收益作为参考）
crash_spx_return = not na(spx_close) and not na(spx_close[crash_return_bars]) ? (spx_close - spx_close[crash_return_bars]) / spx_close[crash_return_bars] * 100 : 0.0
strong_spx_return = not na(spx_close) and not na(spx_close[strong_return_bars]) ? (spx_close - spx_close[strong_return_bars]) / spx_close[strong_return_bars] * 100 : 0.0
dip_spx_return = not na(spx_close) and not na(spx_close[dip_return_bars]) ? (spx_close - spx_close[dip_return_bars]) / spx_close[dip_return_bars] * 100 : 0.0

if final_crash_buy[crash_return_bars] and is_within_stats_range[crash_return_bars]
    crash_buy_return_sum += crash_spx_return
    crash_buy_return_count += 1
if final_strong_buy[strong_return_bars] and is_within_stats_range[strong_return_bars]
    strong_buy_return_sum += strong_spx_return
    strong_buy_return_count += 1
if final_buy_dip[dip_return_bars] and is_within_stats_range[dip_return_bars]
    buy_dip_return_sum += dip_spx_return
    buy_dip_return_count += 1

// 计算平均收益
crash_buy_avg_return = crash_buy_return_count > 0 ? crash_buy_return_sum / crash_buy_return_count : 0.0
strong_buy_avg_return = strong_buy_return_count > 0 ? strong_buy_return_sum / strong_buy_return_count : 0.0
buy_dip_avg_return = buy_dip_return_count > 0 ? buy_dip_return_sum / buy_dip_return_count : 0.0

// 最终信号翻译
signal_txt = ""
signal_col = color.gray
signal_emoji = ""
// Mobile模式用：原始信号和过滤状态分离
mobile_bias_txt = ""
mobile_bias_emoji = ""
mobile_bias_col = color.gray
mobile_status_txt = ""
mobile_status_emoji = ""
mobile_status_col = color.gray
is_filtered = false

if score >= 6
    signal_txt := "🚨 CRASH BUY"
    signal_col := color.new(#00FF00, 0)
    signal_emoji := "🚨"
    mobile_bias_txt := "CRASH BUY"
    mobile_bias_emoji := "🚨"
    mobile_bias_col := color.new(#00FF00, 0)
else if score >= 5
    signal_txt := "STRONG BUY"
    signal_col := color.green
    signal_emoji := "🟢"
    mobile_bias_txt := "STRONG BUY"
    mobile_bias_emoji := "🟢"
    mobile_bias_col := color.green
else if score >= min_score_buy
    mobile_bias_txt := "BUY DIP"
    mobile_bias_emoji := "🟡"
    mobile_bias_col := color.lime
    if vix_regime == 3
        signal_txt := "WAIT (High Vol)"
        signal_col := color.gray
        signal_emoji := "✋"
        is_filtered := true
        mobile_status_txt := "WAIT: High Vol"
        mobile_status_emoji := "✋"
        mobile_status_col := color.yellow
    else if use_momentum_confirm and not is_buy_dip_momentum_ok
        signal_txt := "WAIT (Momentum)"
        signal_col := color.gray
        signal_emoji := "✋"
        is_filtered := true
        mobile_status_txt := "WAIT: Momentum"
        mobile_status_emoji := "✋"
        mobile_status_col := color.yellow
    else
        signal_txt := "BUY DIP"
        signal_col := color.lime
        signal_emoji := "🟡"
else if score <= -6
    signal_txt := "🔥 EUPHORIA SELL"
    signal_col := color.new(#FF0000, 0)
    signal_emoji := "🔥"
    mobile_bias_txt := "EUPHORIA"
    mobile_bias_emoji := "🔥"
    mobile_bias_col := color.new(#FF0000, 0)
else if score <= -5
    signal_txt := "STRONG SELL"
    signal_col := color.red
    signal_emoji := "🔴"
    mobile_bias_txt := "STRONG SELL"
    mobile_bias_emoji := "🔴"
    mobile_bias_col := color.red
else if score <= -2
    mobile_bias_txt := "SELL/HEDGE"
    mobile_bias_emoji := "🟠"
    mobile_bias_col := color.orange
    if vix_regime == 1
        signal_txt := "HOLD (Low Vol)"
        signal_col := color.gray
        signal_emoji := "☕"
        is_filtered := true
        mobile_status_txt := "HOLD: Low Vol"
        mobile_status_emoji := "☕"
        mobile_status_col := color.orange
    else if use_momentum_confirm and not is_sell_hedge_momentum_ok
        signal_txt := "HOLD (Momentum)"
        signal_col := color.gray
        signal_emoji := "✋"
        is_filtered := true
        mobile_status_txt := "HOLD: Momentum"
        mobile_status_emoji := "✋"
        mobile_status_col := color.orange
    else
        signal_txt := "SELL/HEDGE"
        signal_col := color.orange
        signal_emoji := "🟠"
else
    signal_txt := "WAIT / HOLD"
    signal_col := color.gray
    signal_emoji := "⚪"
    mobile_bias_txt := "NEUTRAL"
    mobile_bias_emoji := "⚪"
    mobile_bias_col := color.gray

// 熊市特殊提示
if use_trend_filter and not is_bull_market and score >= min_score_buy and score < 5
    signal_txt := "NO TRADE (Trend)"
    signal_col := color.gray
    signal_emoji := "🚫"
    is_filtered := true
    mobile_status_txt := "NO TRADE: Trend"
    mobile_status_emoji := "🚫"
    mobile_status_col := color.gray

// ========== 7. 绘图 ==========
bgcolor(contango_pct > 0 ? color.new(color.green, 95) : color.new(color.red, 92))

col_z = z >= 0 ? color.from_gradient(z, 0, 3, color.new(color.blue, 40), color.new(color.blue, 10)) : 
                 color.from_gradient(z, -3, 0, color.new(color.red, 10), color.new(color.orange, 40))

plot(show_z ? z : na, color=col_z, title="Term Struct Z", linewidth=2)
plot(show_skew ? scaled_skew : na, color=color.new(color.fuchsia, 40), title="SKEW (Scaled)", linewidth=1)

hline(0, "Zero", color=color.gray, linestyle=hline.style_solid)
mid_hline_style = mid_line_style == "Solid" ? hline.style_solid : mid_line_style == "Dashed" ? hline.style_dashed : hline.style_dotted
hline(1, "+1 Mid", color=mid_line_color, linestyle=mid_hline_style)
hline(-1, "-1 Mid", color=mid_line_color, linestyle=mid_hline_style)
plot(thresh_strong, "Upper", color=color.new(color.gray, 50), style=plot.style_line)
plot(-thresh_strong, "Lower", color=color.new(color.gray, 50), style=plot.style_line)

// 使用滚动最大值的成交量可视化
scaled_vol = not na(vx1_vol) and vol_max_rolling > 0 ? ((vx1_vol * vol_visual_boost) / vol_max_rolling) * 1.8 - 3.8 : na
plot(show_volume ? scaled_vol : na, color=col_vol, title="Smart Volume", style=plot.style_columns, linewidth=2)

// ========== 8. 三级信号图表标签 (v7.0 新增) ==========
plotshape(final_crash_buy ? -thresh_strong - 0.3 : na, title="Crash Buy Signal", location=location.absolute, style=shape.labelup, color=color.new(#00FF00, 0), text="🚨", textcolor=color.white, size=size.small)
plotshape(final_strong_buy ? -thresh_strong - 0.3 : na, title="Strong Buy Signal", location=location.absolute, style=shape.labelup, color=color.new(color.green, 20), text="🟢", textcolor=color.white, size=size.tiny)
plotshape(final_buy_dip ? -thresh_strong - 0.3 : na, title="Buy Dip Signal", location=location.absolute, style=shape.labelup, color=color.new(color.lime, 20), text="🟡", textcolor=color.white, size=size.tiny)

plotshape(is_euphoria_sell ? thresh_strong + 0.3 : na, title="Euphoria Sell Signal", location=location.absolute, style=shape.labeldown, color=color.new(#FF0000, 0), text="🔥", textcolor=color.white, size=size.small)
plotshape(is_strong_sell ? thresh_strong + 0.3 : na, title="Strong Sell Signal", location=location.absolute, style=shape.labeldown, color=color.new(color.red, 20), text="🔴", textcolor=color.white, size=size.tiny)
plotshape(is_sell_hedge ? thresh_strong + 0.3 : na, title="Sell/Hedge Signal", location=location.absolute, style=shape.labeldown, color=color.new(color.orange, 20), text="🟠", textcolor=color.white, size=size.tiny)

// ========== 9. 增强版仪表盘 (Compact/Full 模式) ==========
// 仪表盘位置转换函数 (v7.1 新增)
get_table_pos(pos) => pos == "Top Left" ? position.top_left : pos == "Bottom Right" ? position.bottom_right : pos == "Bottom Left" ? position.bottom_left : pos == "Top Center" ? position.top_center : pos == "Bottom Center" ? position.bottom_center : position.top_right

// Max Profit Stats 计算 (提取到外部以避免警告)
max_profit_crash_price = ta.highest(spx_close, crash_return_bars)
max_profit_strong_price = ta.highest(spx_close, strong_return_bars)
max_profit_dip_price = ta.highest(spx_close, dip_return_bars)
max_profit_crash_ret = not na(spx_close[crash_return_bars]) ? (max_profit_crash_price - spx_close[crash_return_bars]) / spx_close[crash_return_bars] * 100 : 0.0
max_profit_strong_ret = not na(spx_close[strong_return_bars]) ? (max_profit_strong_price - spx_close[strong_return_bars]) / spx_close[strong_return_bars] * 100 : 0.0
max_profit_dip_ret = not na(spx_close[dip_return_bars]) ? (max_profit_dip_price - spx_close[dip_return_bars]) / spx_close[dip_return_bars] * 100 : 0.0

// 如果是 Live Mode，额外增加一行用于显示警告 (仅 Compact/Full 模式)
warning_row_count = backtest_mode or is_mobile ? 0 : 1
// Max Profit Stats 额外行数
max_profit_row_count = use_max_profit_stats and not is_compact and not is_mobile ? 1 : 0
// VIX Characteristics 额外行数 (v7.5 新增)
vix_char_row_count = show_vix_characteristics and not is_mobile ? (is_compact ? 1 : 5) : 0
// Mobile: 3 rows, Compact: 10 rows, Full: 15-17 rows max (depending on VVIX and Max Profit)
panel_rows = is_mobile ? 3 : (is_compact ? 10 : (use_vvix ? 16 : 15)) + warning_row_count + max_profit_row_count + vix_char_row_count
var table panel = table.new(get_table_pos(table_position), 2, panel_rows, border_width=0, frame_color=color.new(color.gray, 100), border_color=color.new(color.gray, 100))

if show_table and barstate.islast
    transp_header = math.max(0, bg_transp - 20)
    transp_cell   = bg_transp
    
    bg_header = color.new(color.black, transp_header) 
    bg_cell   = color.new(color.black, transp_cell)
    txt_lbl   = color.new(color.white, 10)

    mode_display = sens_mode == "High (Scalping)" ? "(High)" : sens_mode == "Normal (Swing)" ? "(Normal)" : "(Trend Guard)"

    // ===== Mobile Mode: 紧凑3行显示 (宽度自适应) =====
    if is_mobile
        // Row 0: Overall Bias + Score (例: "🟢 STRONG BUY +5")
        mobile_row0 = mobile_bias_emoji + " " + mobile_bias_txt + " " + format_signed_int(math.round(score))
        table.cell(panel, 0, 0, mobile_row0, bgcolor=bg_header, text_color=mobile_bias_col, text_size=t_size)
        table.cell(panel, 1, 0, "", bgcolor=color.new(color.black, 100), text_size=t_size, width=0)
        // Row 1: Status (例: "✅ Z:-2.30" 或 "✋ WAIT: High Vol")
        mobile_row1 = is_filtered ? (mobile_status_emoji + " " + mobile_status_txt) : ("✅ Z:" + str.tostring(z, "#.##"))
        mobile_row1_col = is_filtered ? mobile_status_col : color.lime
        table.cell(panel, 0, 1, mobile_row1, bgcolor=bg_cell, text_color=mobile_row1_col, text_size=t_size)
        table.cell(panel, 1, 1, "", bgcolor=color.new(color.black, 100), text_size=t_size, width=0)
        // Row 2: VIX Regime + Value (例: "🟡 VIX:18 NORMAL")
        vix_regime_emoji = vix_regime == 3 ? "🔴" : vix_regime == 1 ? "🟢" : "🟡"
        vix_regime_short = vix_regime == 3 ? "HIGH" : vix_regime == 1 ? "LOW" : "NORMAL"
        mobile_row2 = vix_regime_emoji + " VIX:" + str.tostring(vix, "#.#") + " " + vix_regime_short
        table.cell(panel, 0, 2, mobile_row2, bgcolor=bg_cell, text_color=vix_regime_color, text_size=t_size)
        table.cell(panel, 1, 2, "", bgcolor=color.new(color.black, 100), text_size=t_size, width=0)

    // ===== Compact / Full Mode =====
    if not is_mobile
        table.cell(panel, 0, 0, "AI Signal " + mode_display, bgcolor=bg_header, text_color=color.white, text_size=t_size, width=12)
        table.cell(panel, 1, 0, is_compact ? "Compact" : "Full", bgcolor=bg_header, text_color=color.white, text_size=t_size, width=9)

        // Row 1: Overall Bias
        table.cell(panel, 0, 1, "Overall Bias", bgcolor=bg_cell, text_color=color.white, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, 1, signal_txt, bgcolor=bg_cell, text_color=signal_col, text_size=t_size)

        // Row 2: AI Score
        score_col = score >= 5 ? color.green : score >= min_score_buy ? color.lime : score <= -5 ? color.red : score <= -2 ? color.orange : color.silver
        table.cell(panel, 0, 2, "AI Score", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, 2, str.tostring(score, "#.#"), bgcolor=bg_cell, text_color=score_col, text_size=t_size)

        // Row 3: Market Trend (双指数合并显示)
        table.cell(panel, 0, 3, "Market Trend", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, 3, trend_status_txt, bgcolor=bg_cell, text_color=trend_color, text_size=t_size)

        // Row 4: VIX Regime
        table.cell(panel, 0, 4, "VIX Regime", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, 4, vix_regime_txt + " (" + str.tostring(vix, "#.#") + ")", bgcolor=bg_cell, text_color=vix_regime_color, text_size=t_size)

        // Row 5: Term Struct Z
        z_col_t = z > thresh_strong ? color.new(color.blue, 20) : z < -thresh_strong ? color.new(color.red, 20) : color.silver
        table.cell(panel, 0, 5, "Term Struct Z", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, 5, str.tostring(z, "#.##"), bgcolor=bg_cell, text_color=z_col_t, text_size=t_size)

        // Row 6: Vol Status
        vol_ratio = not na(vx1_vol) ? vx1_vol / vol_ma : 0
        vol_txt = vol_ratio > 2.5 ? "PANIC SPIKE" : vol_ratio > 1.5 ? "High Activity" : "Normal"
        vol_col_txt = vol_ratio > 2.5 ? color.yellow : vol_ratio > 1.5 ? color.aqua : color.silver
        table.cell(panel, 0, 6, "Vol Status", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, 6, vol_txt, bgcolor=bg_cell, text_color=vol_col_txt, text_size=t_size)

        // Row 7: Z Momentum
        z_mom_txt = z_momentum_bullish ? "↓ Falling" : z_momentum_bearish ? "↑ Rising" : "→ Flat"
        z_mom_col = z_momentum_bullish ? color.lime : z_momentum_bearish ? color.orange : color.silver
        table.cell(panel, 0, 7, "Z Momentum", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, 7, z_mom_txt, bgcolor=bg_cell, text_color=z_mom_col, text_size=t_size)

        // Row 8: Signal Stats (v7.0 新增)
        if show_signal_stats
            stats_txt = "🚨:" + str.tostring(crash_buy_count) + " 🟢:" + str.tostring(strong_buy_count) + " 🟡:" + str.tostring(buy_dip_count)
            table.cell(panel, 0, 8, "Signal Stats", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, 8, stats_txt, bgcolor=bg_cell, text_color=color.white, text_size=t_size)

        // Row 9: VIX Characteristics (v7.5 新增 - Compact模式汇总)
        if show_vix_characteristics and is_compact
            char_summary = format_signed_int(vix_characteristics_total) + " BD:" + str.tostring(backwardation_days) + "D " + (is_bullish_divergence ? "🟢" : is_bearish_divergence ? "🔴" : "→")
            char_color = vix_characteristics_total >= 3 ? color.lime : vix_characteristics_total <= -2 ? color.red : color.silver
            table.cell(panel, 0, 9, "VIX Chars", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, 9, char_summary, bgcolor=bg_cell, text_color=char_color, text_size=t_size)

    // ===== Full Mode Only =====
    if not is_compact and not is_mobile
        base_row = show_signal_stats ? 9 : 8
        
        // Avg Return 分级显示 (v7.1 新增)
        if show_signal_stats
            // 第一行：CRASH 和 STRONG
            avg_return_txt1 = "🚨" + str.tostring(crash_return_bars) + "D:" + format_signed_1(crash_buy_avg_return) + "% 🟢" + str.tostring(strong_return_bars) + "D:" + format_signed_1(strong_buy_avg_return) + "%"
            table.cell(panel, 0, base_row, "Avg Return", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, base_row, avg_return_txt1, bgcolor=bg_cell, text_color=color.white, text_size=t_size)
            // 第二行：BUY DIP
            avg_return_txt2 = "🟡" + str.tostring(dip_return_bars) + "D:" + format_signed_1(buy_dip_avg_return) + "%"
            table.cell(panel, 0, base_row + 1, "", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, base_row + 1, avg_return_txt2, bgcolor=bg_cell, text_color=color.white, text_size=t_size)
            base_row += 2

        // VIX Basis
        basis_col = is_basis_panic ? color.red : is_basis_calm ? color.lime : color.silver
        basis_txt = str.tostring(vix_basis, "#.##") + "%"
        table.cell(panel, 0, base_row, "VIX Basis", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, base_row, basis_txt, bgcolor=bg_cell, text_color=basis_col, text_size=t_size)

        // Contango
        c_col = contango_pct > 0 ? color.lime : color.red
        table.cell(panel, 0, base_row + 1, "Contango %", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, base_row + 1, str.tostring(contango_pct, "#.##") + "%", bgcolor=bg_cell, text_color=c_col, text_size=t_size)

        // SKEW
        skew_val_str = not na(skew) ? str.tostring(skew, "#.0") : "N/A"
        skew_col_final = not na(skew) ? (is_skew_high ? color.red : is_skew_low ? color.lime : color.silver) : color.gray
        table.cell(panel, 0, base_row + 2, "SKEW Index", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, base_row + 2, skew_val_str, bgcolor=bg_cell, text_color=skew_col_final, text_size=t_size)

        // VVIX (if enabled)
        vvix_offset = 0
        if use_vvix
            vvix_val_str = not na(vvix) ? str.tostring(vvix, "#.#") : "N/A"
            vvix_col = is_vvix_panic ? color.red : is_vvix_calm ? color.lime : color.silver
            table.cell(panel, 0, base_row + 3, "VVIX", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, base_row + 3, vvix_val_str, bgcolor=bg_cell, text_color=vvix_col, text_size=t_size)
            vvix_offset := 1

        // P/C Ratio
        pcr_display = not na(pcr) ? str.tostring(pcr, "#.##") : "N/A"
        pcr_col = not na(pcr) ? (pcr > 1.2 ? color.lime : pcr < 0.7 ? color.red : color.silver) : color.gray
        table.cell(panel, 0, base_row + 3 + vvix_offset, "Put/Call (Idx)", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
        table.cell(panel, 1, base_row + 3 + vvix_offset, pcr_display, bgcolor=bg_cell, text_color=pcr_col, text_size=t_size)
        
        // Max Profit Stats (v7.1 新增 - 可选)
        if use_max_profit_stats
            max_ret_txt = "🚨" + format_signed_1(max_profit_crash_ret) + "% 🟢" + format_signed_1(max_profit_strong_ret) + "% 🟡" + format_signed_1(max_profit_dip_ret) + "%"
            table.cell(panel, 0, base_row + 4 + vvix_offset, "Max Profit", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, base_row + 4 + vvix_offset, max_ret_txt, bgcolor=bg_cell, text_color=color.new(color.aqua, 20), text_size=t_size)

        // VIX Characteristics 详情行 (v7.5 新增 - Full模式)
        if show_vix_characteristics
            max_profit_offset = use_max_profit_stats ? 1 : 0
            char_base_row = base_row + 4 + vvix_offset + max_profit_offset

            // Mean Reversion
            mr_txt = format_signed_1(vix_mean_distance) + "σ (" + format_signed_int(mean_reversion_score) + ")"
            mr_col = vix_mean_distance > 2 ? color.lime : vix_mean_distance < -1 ? color.red : color.silver
            table.cell(panel, 0, char_base_row, "Mean Revert", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, char_base_row, mr_txt, bgcolor=bg_cell, text_color=mr_col, text_size=t_size)

            // Backwardation Duration
            table.cell(panel, 0, char_base_row + 1, "Backwardat.", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, char_base_row + 1, backwardation_status, bgcolor=bg_cell, text_color=backwardation_color, text_size=t_size)

            // Panic Status
            table.cell(panel, 0, char_base_row + 2, "Panic Status", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, char_base_row + 2, panic_status, bgcolor=bg_cell, text_color=panic_status_color, text_size=t_size)

            // VIX-SPX Divergence
            table.cell(panel, 0, char_base_row + 3, "VIX-SPX Div", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, char_base_row + 3, divergence_status, bgcolor=bg_cell, text_color=divergence_status_color, text_size=t_size)

            // Vol Cluster
            cluster_txt = vol_state_txt + " (" + str.tostring(vol_cluster_ratio, "#.#") + "x)"
            table.cell(panel, 0, char_base_row + 4, "Vol Cluster", bgcolor=bg_cell, text_color=txt_lbl, text_halign=text.align_left, text_size=t_size)
            table.cell(panel, 1, char_base_row + 4, cluster_txt, bgcolor=bg_cell, text_color=vol_state_color, text_size=t_size)

    // 重绘警告行 (v7.1 新增) - Live Mode 显示警告 (仅 Compact/Full 模式)
    if not backtest_mode and not is_mobile
        warning_row_idx = panel_rows - 1
        table.cell(panel, 0, warning_row_idx, "⚠️ Live Mode", bgcolor=color.new(color.orange, 85), text_color=color.orange, text_size=size.tiny, text_halign=text.align_left)
        table.cell(panel, 1, warning_row_idx, "Historical Repaints", bgcolor=color.new(color.orange, 85), text_color=color.orange, text_size=size.tiny)

// ========== 10. SMART ALERT (Unified Alert System) ==========
// 基于 Adaptive RSI 的智能警报：信号聚合 + 上升沿检测 + VIX Regime 自适应冷却
// 高波动期(HIGH VOL)冷却减半 → 市场变化快，需要更及时的信号
// 低波动期(LOW VOL)冷却加倍 → 市场平静，减少不必要通知

var int last_buy_alert_bar = 0
var int last_sell_alert_bar = 0

// VIX Regime 自适应冷却：高波动减半，低波动加倍
adaptive_cooldown = vix_regime == 3 ? math.round(alert_cooldown_base / 2) : vix_regime == 1 ? alert_cooldown_base * 2 : alert_cooldown_base

if enable_smart_alert
    // ===== 买入信号聚合 =====
    has_buy_signal = final_crash_buy or final_strong_buy or final_buy_dip
    has_buy_signal_prev = (final_crash_buy[1] == true) or (final_strong_buy[1] == true) or (final_buy_dip[1] == true)
    is_new_buy = has_buy_signal and not has_buy_signal_prev
    
    if is_new_buy and (bar_index - last_buy_alert_bar > adaptive_cooldown)
        // 构建消息头
        msg = str.format("{0}: 🟢 BUY → ", syminfo.ticker)
        
        // 添加触发的信号标签
        if final_crash_buy
            msg := msg + "🚨CRASH "
        if final_strong_buy
            msg := msg + "🟢STRONG "
        if final_buy_dip
            msg := msg + "🟡DIP "
        
        // 添加上下文：Score, Z, VIX Regime, 指数趋势
        msg := msg + str.format("| Score:{0} Z:{1,number,#.##} VIX:{2,number,#.#}({3}) {4}",
            score, z, vix, vix_regime_txt, trend_status_txt)

        // v7.5 新增：VIX特性上下文
        if use_vix_characteristics
            msg := msg + str.format(" | Char:{0} BD:{1}D {2}",
                vix_characteristics_total, backwardation_days, divergence_status)

        alert(msg, alert_freq)
        last_buy_alert_bar := bar_index

    // ===== 卖出信号聚合 =====
    has_sell_signal = is_euphoria_sell or is_strong_sell or is_sell_hedge
    has_sell_signal_prev = (is_euphoria_sell[1] == true) or (is_strong_sell[1] == true) or (is_sell_hedge[1] == true)
    is_new_sell = has_sell_signal and not has_sell_signal_prev
    
    if is_new_sell and (bar_index - last_sell_alert_bar > adaptive_cooldown)
        msg = str.format("{0}: 🔴 SELL → ", syminfo.ticker)
        
        if is_euphoria_sell
            msg := msg + "🔥EUPHORIA "
        if is_strong_sell
            msg := msg + "🔴STRONG "
        if is_sell_hedge
            msg := msg + "🟠HEDGE "
        
        // 添加上下文：Score, Z, SKEW, 指数趋势
        msg := msg + str.format("| Score:{0} Z:{1,number,#.##} SKEW:{2,number,#} {3}",
            score, z, skew, trend_status_txt)

        // v7.5 新增：VIX特性上下文
        if use_vix_characteristics
            msg := msg + str.format(" | Char:{0} {1} {2}",
                vix_characteristics_total, vol_state_txt, divergence_status)

        alert(msg, alert_freq)
        last_sell_alert_bar := bar_index